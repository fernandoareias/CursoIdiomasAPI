using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace CursoIdiomas.Tests
{
    [TestClass]
    public class TeacherTests
    {
        // Deve retornar se professor for valido
        [TestMethod]
        public void ShouldReturnIfTeacherIsValid()
        {
            Assert.Fail();
        }

        // Deve retornar se professor for invalido
        [TestMethod]
        public void ShouldReturnIfTeacherIsInvalid()
        {
            Assert.Fail();
        }
    }
}
