using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace CursoIdiomas.Tests
{
    [TestClass]
    public class ClassesTests
    {
        // Deve retorna se a turma for valida
        [TestMethod]
        public void ShouldReturnIfClasseIsValid()
        {
            Assert.Fail();
        }

        // Deve retorna se a turma for invalida.
        [TestMethod]
        public void ShouldReturnIfClasseIsInvalid()
        {
            Assert.Fail();
        }
    }
}
